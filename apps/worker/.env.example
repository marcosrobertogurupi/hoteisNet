DATABASE_URL="postgresql://user:password@host:6543/postgres?schema=public&pgbouncer=true"
# URL pública do app web — usada para montar o link de pré-check-in/FNRH enviado ao hóspede.
APP_BASE_URL="https://app.hoteis.net"
# Google Generative AI (Gemini) — usada pelo agente de monitoramento para compor resumos em
# linguagem natural. Mesma chave usada pelo apps/web (ver apps/web/.env.local).
GOOGLE_GENERATIVE_AI_API_KEY="AQ...."
