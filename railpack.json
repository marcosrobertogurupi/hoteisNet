{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": ["npm run build --workspace=@hoteisnet/worker"]
    }
  },
  "deploy": {
    "startCommand": "npm run start --workspace=@hoteisnet/worker"
  }
}
